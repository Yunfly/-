{>"layouts/master" /}

{<body}
	{#books}
		<div class="large-3 colums book">
		<img src="img/{.cover}" />
		<h4>{.title}</h4>
		<p>{.turnText}</p> 
		<div class="price">Buy it for <span>{.price}</span></div>
		<br>
		<a class='button small radius' href='/books/details/{._id}'>Book Details</a>
	</div>
	{/books}
	
{/body}
